// import { Jekyll } from '@bowtie/houndstooth-sdk'
// import storage from './storage'

// const github = new GitHub({ token: storage.get('access_token') })

// storage.on('access_token_changed', (token) => {
//   github.auth(token)
// })

// export default github
